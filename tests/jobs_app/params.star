param("port", type=INT, description="The port the flask app is listening on (inside the container)", default=5000)

param("greeting", description="Greeting printed by the hello and star jobs", default="hi")
